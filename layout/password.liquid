<!doctype html>
<html lang="{{ shop.locale }}">
<head>
  {% include 'theme-meta-tags' %}
  {% include 'theme-data-layer' %}
  {% include 'theme-css' %}
  {% include 'theme-js' %}
  {{ content_for_header }}
  {% include 'theme-third-party-scripts' %}
</head>
<body class="template-{{ template | replace: '.', '-' }}">
  <main id="main">
    {{ content_for_layout }}
  </main>
  <span class="screenreader" id="screenreader-announce" aria-live="polite"></span>
</body>
</html>
